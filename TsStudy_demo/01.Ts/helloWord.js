"use strict";
function getPersonInfor(person) {
    console.log("name: " + person.name + "; age:" + person.age);
    return "name: " + person.name + "; age:" + person.age;
}
var vera = {
    name: 'Guess',
    age: 20
};
getPersonInfor(vera);
